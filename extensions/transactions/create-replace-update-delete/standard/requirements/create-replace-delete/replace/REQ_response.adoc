[[req_create-replace-delete_put-response]]   
[width="90%",cols="2,6a"]
|===
^|*Requirement {counter:req-id}* |*/req/create-replace-delete/put-response*    
^|A |If the operation is executed immediately and has replaced the content of an existing resource, a successful execution of the operation SHALL be reported as a response with a HTTP status code `200` or `204`.
^|B |If the operation is executed immediately and has created a new resource, a successful execution of the operation SHALL be reported as a response with a HTTP status code `201` (see <<rfc9110,section 9.3.4 of RFC 9110>>).
^|C |If the operation is not executed immediately, but is accepted for processing at a later time (see Permission <<per_create-replace-delete_async,/per/create-replace-delete/async>>), the response SHALL have a HTTP status code `202`.
|===
