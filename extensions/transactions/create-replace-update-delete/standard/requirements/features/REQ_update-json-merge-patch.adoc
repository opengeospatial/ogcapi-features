[[req_features_update-json-merge-patch]]
[width="90%",cols="2,6a"]
|===
^|*Requirement {counter:req-id}* |*/req/features/update-json-merge-patch*
^|Condition |Server implements <<update_clause>>
^|Condition |Server implements <<OAFeat-5,OGC API - Features - Part 5: Schemas, Requirements Class "Returnables and Receivables">>
^|Condition |Server supports the media type `application/merge-patch+json` in the request body of UPDATE operations for a feature collection `collectionId`
^|Condition |Server accepts GeoJSON (with or without JSON-FG extensions) as the payload of a POST or PUT request (see <<req_features_geojson-create-replace,/req/features/geojson-create-replace>>)
^|A |The server SHALL process PATCH requests with a content type `application/merge-patch+json` to such a resource endpoint as specified by <<rfc7396,RFC 7396 (JSON Merge Patch)>> under the assumption that the feature is represented in GeoJSON (with or without JSON-FG extensions) according to the JSON Schema describing the Returnables and Receivables.
^|B |The value of the "place" or "geometry" member SHALL be mapped to the property with the role "primary-geometry". If the "place" member is present and its value is not `null`, that value SHALL take precedence.
^|C |The server SHALL reject requests where the "id" member, that is, the property with `x-ogc-role` set to "id" in the schema, is updated.
^|D |The server SHALL process requests that update a spatial property under the assumption that the value is a GeoJSON geometry object or a JSON-FG geometry object, depending on the GeoJSON profile declared in the request (see <<req_features_geojson-jsonfg,/req/features/geojson-jsonfg>>).
^|E |The server SHALL unset properties that have the value `null`.
|===
