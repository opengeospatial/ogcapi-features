---
description:
  A list of properties by which the server response may be sorted.
content:
  application/json:
    schema:
      $ref: '../schemas/schema.yaml'
  text/html:
    schema:
      type: string 
